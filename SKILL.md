---
name: equity-analysis-dashboard
description: 当用户提供股权/财务梳理类 Excel（港股 18A 生物医药、A 股未盈利公司、PE/VC 被投企业等），并要求"做投资分析看板 / 数据可视化 / 少数股东视角分析 / 解禁减持逻辑拆解 / 方案逻辑拆解"时使用。把 Excel 数据生成自包含、离线可用的 ECharts HTML 看板（股价+成交量、融资估值轨迹、股东结构、持股成本 vs 现价、解禁窗口、可比公司），并套用"方案逻辑拆解"框架（逻辑链图 + 减持工具对比 + 关键判断），服务少数股东 / PE 退出决策。
agent_created: true
---

# 股权 / 投资分析看板生成器

## Overview

把一个「股权梳理 / 财务 / 交易 / 可比公司」类的 Excel，转换成**单文件、离线、自包含**的 HTML 投资分析看板（内嵌一份 ECharts，无需联网）。看板以**少数股东 / PE 视角**组织，并内置一套可复用的**「方案逻辑拆解」框架**（逻辑链图 + 减持工具对比表 + 关键判断），用于解禁 / 减持 / 退出决策分析。

核心原则：所有数值必须由脚本从原始 Excel 确定性提取，缺失资料不得推测或编造；图注必须与图上实际绘制的系列一致。

## When to use

- 用户给了一个 Excel（或指向一个本地 xlsx），内容像「股权信息梳理 / 财务数据分析 / 股东表 / 融资情况 / 上市后交易 / 可比公司」。
- 用户用到的动词：`做看板 / 出图 / 可视化 / 投资分析 / 少数股东视角 / 重点看什么 / 解禁 / 减持 / 方案逻辑拆解`。
- 用户要求把多张图合并成一个网页、或把"方案/提案逻辑"拆成图。
- 用户是私募 / PE / 少数股东，关心退出、解禁抛压、流动性、控制权。

不适用：纯文字报告 / PPT / 非结构化数据 / 需要后端的数据看板。

## Workflow（概要）

详细步骤、代码模式与坑点见 `references/workflow.md`；逻辑链框架见 `references/logic-chain.md`。

1. **探查 Excel**：用 pandas 读出每个 sheet，识别日期列、数值列、表头位置。港股 18A 常无三表，用"股价 / 融资 / 估值 / 流动性"作代理指标并显式标注。
2. **提取 6 类核心数据**（按公司实际情况裁剪）：
   - 上市后股价+成交量序列（双轴：收盘价 line + 成交量 bar）
   - 历史融资估值轨迹（每股成本折线，标出峰值 / down-round）
   - 股东持股结构（横向条形，按合计占比）
   - 主要股东持股成本 vs 现价（横向条形 + 现价参考线）
   - 解禁窗口（前 8 日 + 解禁日 + 后 8 日，收盘价 + 当日涨跌幅，突出"悬崖在解禁后"）
   - 可比公司（较首发收盘价涨跌幅，本公司红色高亮 + 表格）
3. **装配单文件 HTML 看板**：KPI 卡 + sticky 导航 + 图表卡（每图"保存图片"按钮）+ 少数股东分析卡（8 项框架要点）。模板与图表 option 用 `__TOKEN__` 链式 replace 注入（CSS 含 `%` 单位，不能用 `%` 占位符格式化）。
4. **融入「方案逻辑拆解」框架**（可选，通常放在看板末尾）：逻辑链图（ECharts graph，layout:'none' 固定坐标）+ 港股减持工具对比表 + 关键判断黄条。范式见 `references/logic-chain.md`。
5. **校验**：`node --check` 验证脚本语法；用 `echarts.init` 桩（见 `scripts/validate.js`）跑一遍确认所有图都能 `setOption`，逻辑链节点数 / 连线数正确、tooltip formatter 确为函数。
6. **交付**：产物为 `xxx投资分析看板.html`（自包含，约 1MB）。把 `assets/echarts.min.js` 复制进输出目录供构建脚本内嵌。

## Resources

### references/
- `workflow.md` — 完整流程、pandas 提取模式、6 类图表 option 模板、合并技巧、已知坑（中文名重叠 / 子表列序差异 / 解禁日探测 / 函数注入）。
- `logic-chain.md` — 「方案逻辑拆解」框架规格：6 类节点定义、节点/连线设计、graph option 模板与"tooltip formatter 函数注入"技巧、坐标适配规则。

### scripts/
- `build_dashboard.py` — 可运行的模板构建脚本（改编自真实案例，含清晰 TODO 标注）。复制后按目标公司数据修改 `EXCEL` 路径与各提取段即可。
- `validate.js` — Node 校验桩：从生成的 HTML 抽取 init 脚本，mock `echarts/document/window/ResizeObserver`，跑全部 `setOption` 并校验逻辑链图。
- `fetch_echarts.py` — 下载 ECharts 5.4.4 到 `assets/`（首次使用跑一次）。

### assets/
- `echarts.min.js` — ECharts 5.4.4 离线库（构建脚本内嵌进看板）。约 1MB，未入库，首次使用先跑 `scripts/fetch_echarts.py` 下载。

## Known pitfalls（速查，详见 references）

- **长中文系列名 / 轴名会挤爆图例与坐标轴，导致重叠截断** → 用短显示名（经营/投资/筹资、研发费率、阶段一/阶段二等），≥3 系列图例放左侧垂直排列。
- **可比公司子表列序常异于主表**（子表 col1=收盘价、col2=成交量；主表 col3=收盘、col5=成交量）→ 提取前先打印列，避免把成交额当收盘价。
- **graph 的 tooltip.formatter 不能 json.dumps 成函数** → 占位符 `"__TOOLTIP_FN__"` 经 `json.dumps` 后再字符串替换为函数体。
- **graph 用 layout:'none' 时 x/y 是像素坐标，不会自动缩放** → 整体跨度 + 符号半径必须 ≤ 卡片内宽（约 910px），否则右侧节点被裁切。
- **解禁日**常是禁售承诺最后一日而非首个日期 → 按业务确认，勿机械取首个 date-like 值。
- **港股 18A 未盈利、无三表** → 用可得数据做四维诊断的"代理指标"，并明确标注"非传统财务比率"。
